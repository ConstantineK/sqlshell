# This is probably not a long lived test, but we never had the hard numbers to compare
..\internal\import\smoLibraryImport.ps1

$ServerName = "localhost" # We also need to try with servers where network latency is >100ms

# basic connection

# Disabling some properties, can we test them all individually?

# Disabling all

# Disabling combinations? Worth?
